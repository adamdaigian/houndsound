function hotsSoundCloud(playback_count)
	SC.get('/tracks')
	playback_count: {
		from: 50,000
	}
}

function(tracks){
	var randon = Math.floor(Math.random() * 49);
	SC.oEmbed(tracks[random]).uri, { auto_play; true }, document.getElementById('target'));
  });
}	

window.onload = function () {
	SC.initialize({
	client_id: '45e1f7e473518eccbcb6bc27ecac7c44'
	});

var hero_units = document.getElementsByClassName('playback_count');
for (var i = 0; i < menulinks[i];

}